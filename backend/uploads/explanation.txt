**Before:**

```
function sayHi() {
  console.log("👋 Hello world");
}
```

This code defines a function called `sayHi` that logs "👋 Hello world" to the console.

**After:**

```
sayHi()
```

This code calls the `sayHi` function, causing the message "👋 Hello world" to be printed to the console.